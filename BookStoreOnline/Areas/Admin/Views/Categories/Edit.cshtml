@model BookStoreOnline.Models.LOAI

@{
    ViewBag.Title = "Edit";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}
<div class="card-body">
    <h2>Chỉnh sửa</h2>
</div>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div>

        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Maloai)

        <div class="mb-3">
            <label for="CategoryName" class="form-label">Thể loại</label>
            @Html.EditorFor(model => model.Tenloai, new { htmlAttributes = new { @class = "form-control", @id = "CategoryName" } })
            @Html.ValidationMessageFor(model => model.Tenloai, "", new { @class = "text-danger" })
        </div>

        <div class="d-flex justify-content-between align-items-center">
            @Html.ActionLink("Quay lại", "Index", null, new { @class = "btn btn-secondary", style = "color: white !important;" })
            <button type="submit" class="btn btn-primary">Lưu</button>
        </div>
    </div>
}
 